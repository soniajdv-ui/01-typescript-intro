/*he creado una carpeta dentro de src llamada topics, dentro de esa
carpeta he creado éste archivo*/

const name: string = 'Soniajdv';
/*hpPoints puede ser un número o una palabra como 'FULL',
podemos usar ambos tipos a la vez*/
let hpPoints: number | string = 95;
const isAlive: boolean = true;

console.log({ 
    name, hpPoints, isAlive 
});